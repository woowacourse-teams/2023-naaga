name: NAAGA BACKEND MERGE CI

on:
  push:
    branches: 
      - feat/#222

jobs:
  execute_deployment_script:
    name: backend deploy
    runs-on: naaga
    
    steps:
      - name: Execute script
        run: |
          sudo ./deploy.sh
        working-directory: /home/ubuntu
        
      
